type User {
  id: ID! @id
  name: String!
  writtenKudos: [Kudos] @relation(name: "WrittenKudos")
  ownKudos: [Kudos] @relation(name: "OwnKudos")
}

type Kudos {
  id: ID! @id
  title: String!
  author: User @relation(name: "WrittenKudos")
  recipient: User @relation(name: "OwnKudos")
}
